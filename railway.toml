[build]
builder = "nixpacks"

[deploy]
startCommand = "NODE_ENV=production node --loader tsx/esm server.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
