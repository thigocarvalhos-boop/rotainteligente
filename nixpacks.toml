# nixpacks.toml — força Node.js e instala TODAS as dependências para o build

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production node --loader tsx/esm server.ts"
